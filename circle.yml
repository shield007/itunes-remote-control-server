machine:
  ruby:
    version: 2.0.0
        
test:
  override:
    - ruby --version    
    - ./install-gems.sh    
    - rake all    
    - cp -R doc $CIRCLE_ARTIFACTS/
    - cp *.gem $CIRCLE_ARTIFACTS
  
