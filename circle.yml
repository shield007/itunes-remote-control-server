machine:
  ruby:
    version: 2.0.0
        
test:
  override:
    - ruby --version    
    - ./install-gems.sh
    - rake yard
    - rake all
    - ls -la *
    - cp -R docs $CIRCLE_ARTIFACTS
    - cp *.gem $CIRCLE_ARTIFACTS
  
