machine:
  ruby:
    version: 2.0.0
        
test:
  override:
    - rvm install ruby-2.0.0
    - ./install-gems.sh
    - rake
    - ls -la *
    - cp -R docs $CIRCLE_ARTIFACTS
    - cp *.gem $CIRCLE_ARTIFACTS
  
